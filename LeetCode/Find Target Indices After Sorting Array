def targetIndices(self, nums: List[int], target: int) -> List[int]:
        nums = sorted(nums)
        newArray = []
        for i in range(len(nums)):
            if nums[i] == target:
                newArray.append(i)
        return newArray
