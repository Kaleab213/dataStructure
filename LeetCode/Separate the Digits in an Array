class Solution:
    def separateDigits(self, nums: List[int]) -> List[int]:
        s = ""
        for i in nums:
            s += str(i)
        nums = []
        for i in s:
            nums.append(int(i))
        return nums
